name: transformfit
description: "TransformFit Flutter Web — assistance-based workout coaching."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.12.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.0.0
  go_router: ^14.6.2
  drift: ^2.22.0
  shared_preferences: ^2.5.5
  supabase_flutter: ^2.9.1
  sqlite3_flutter_libs: ^0.5.26
  meta: ^1.9.0
  health: ^13.3.1
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.22.0

flutter:
  uses-material-design: true
  assets:
    - assets/brand/transformfitai-logo-original.jpg
    - assets/imagery/hero_workout.png
    - assets/imagery/hero_mindfulness.png
    - assets/imagery/hero_app_mockup.png
    - assets/imagery/hero_barbell.png
    - assets/imagery/hero_nutrition.png
    - assets/imagery/hero_gamification.png
    - assets/imagery/hero_sleep.png
    - assets/imagery/hero_ai_coach.png
    - assets/imagery/hero_app_store.png
    - assets/imagery/exercise_squat.png
    - assets/imagery/exercise_bench_press.png
    - assets/imagery/exercise_deadlift.png
    - assets/imagery/exercise_pullup.png
    - assets/imagery/exercise_plank.png
    - assets/imagery/exercise_yoga.png
    - assets/imagery/exercise_kettlebell.png
    - assets/imagery/exercise_overhead_press.png
  fonts:
    - family: Playfair
      fonts:
        - asset: assets/fonts/PlayfairDisplay-VariableFont_wght.ttf
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-VariableFont_opsz,wght.ttf
    # The Flutter web engine hardcodes "Roboto" as its internal fallback font
    # and fetches it from fonts.gstatic.com at runtime, even when every app
    # widget resolves to a bundled family (Flutter issue #163554). The only
    # endorsed way to stop that third-party fetch is to register a local
    # "Roboto" family. We alias it to the bundled Inter variable font so the
    # engine's Roboto fallback resolves to a same-origin asset that is already
    # loaded for the Inter family (no extra download, zero gstatic requests),
    # preserving the bundled-fonts-only contract: only Playfair + Inter .ttf
    # are ever fetched, all from the app's own origin.
    - family: Roboto
      fonts:
        - asset: assets/fonts/Inter-VariableFont_opsz,wght.ttf
